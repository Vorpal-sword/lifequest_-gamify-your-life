# Rule-Based System для LifeQuest

---

## 🎯 Мета роботи

Реалізувати rule-based систему в Python з наступними компонентами:

1. ✅ Завантаження бази фактів і правил
2. ✅ Механізм логічного виведення (forward і backward chaining)
3. ✅ Інтеграція з додатком геймифікації життя LifeQuest

---

## 🏗️ Архітектура системи

### Основні компоненти

```
rule_based_system/
│
├── rule_engine.py          # Ядро системи
│   ├── Fact                # Клас факту
│   ├── Condition           # Клас умови
│   ├── Action              # Клас дії
│   ├── Rule                # Клас правила
│   ├── KnowledgeBase       # База знань
│   ├── InferenceEngine     # Механізм виведення
│   └── RuleParser          # Парсер правил
│
├── data/
│   ├── lifequest_rules.json    # 20 правил для LifeQuest
│   └── initial_facts.json      # Початкові факти
│
├── demo.py                 # Демонстрація роботи
├── test_rule_engine.py     # Unit тести
└── README.md              # Документація
```

---

## 🔧 Механізм логічного виведення

### 1. Forward Chaining (Прямий ланцюг)

**Принцип роботи:** Від фактів до висновків

```python
kb, engine = create_rule_based_system()

# Додаємо факти
kb.add_fact(Fact('user_level', 0))
kb.add_fact(Fact('total_tasks_completed', 0))

# Завантажуємо правила
rules = RuleParser.parse_json_rules(rules_data)
kb.add_rules(rules)

# Виконуємо виведення
result = engine.forward_chain()
```

**Алгоритм:**

1. Беремо поточні факти
2. Знаходимо правила, умови яких виконуються
3. Застосовуємо правило з найвищим пріоритетом
4. Додаємо нові факти до бази
5. Повторюємо поки є застосовні правила

### 2. Backward Chaining (Зворотній ланцюг)

**Принцип роботи:** Від цілі до фактів

```python
# Намагаємось довести ціль
result = engine.backward_chain('user_status')

if result['proved']:
    print("Ціль досягнута!")
    print(result['reasoning_path'])
```

**Алгоритм:**

1. Задаємо ціль (цільовий факт)
2. Перевіряємо чи ціль вже є фактом
3. Якщо ні - шукаємо правила, які можуть вивести цю ціль
4. Рекурсивно доводимо підцілі (умови правил)
5. Застосовуємо правило, якщо всі умови доведені

---

## 📊 База знань для LifeQuest

### Категорії правил (20 правил)

#### 1. Рівні та статуси (5 правил)

- `R001`: Новачок (рівень 0, 0 завдань)
- `R002`: Підвищення рівня (100+ XP)
- `R004`: Досвідчений (рівень 2+, 10+ завдань)
- `R011`: Майстер (50+ завдань)
- `R019`: Експерт (200+ завдань, рівень 10+)

#### 2. Досягнення (4 правила)

- `R003`: "Перші кроки" (5 завдань)
- `R007`: "Рання пташка" (3 завдання до 9:00)
- `R016`: "Гармонія життя" (баланс 80+)
- `R020`: Річниця (365 днів)

#### 3. Здоров'я (3 правила)

- `R006`: Нагадування про перерву (4+ години роботи)
- `R010`: Рекомендація фізичної активності (6+ годин сидіння)
- `R013`: Медитація при стресі (рівень 7+)

#### 4. Продуктивність (4 правила)

- `R005`: Продуктивний день (3+ завдання)
- `R008`: Бонус за серію (7+ днів)
- `R014`: Вечірній режим (для "сов")
- `R015`: Мотивація при низькій активності

#### 5. Персоналізація (4 правила)

- `R009`: Командні квести
- `R012`: Пріоритетна категорія
- `R017`: Персональні рекомендації
- `R018`: Щотижневий звіт

### Приклад правила

```json
{
  "id": "R003",
  "priority": 90,
  "description": "Присвоїти досягнення 'Перші кроки' після виконання 5 завдань",
  "conditions": [
    { "fact": "total_tasks_completed", "operator": ">=", "value": 5 },
    { "fact": "has_achievement_first_steps", "operator": "==", "value": false }
  ],
  "actions": [
    { "fact": "has_achievement_first_steps", "value": true },
    { "fact": "achievement_earned", "value": "Перші кроки" },
    { "fact": "user_xp", "value": 25 }
  ]
}
```

---

## 🚀 Використання

### Запуск демонстрації

```bash
python demo.py
```

**Виконує:**

- ✅ Демонстрація прямого ланцюга виведення
- ✅ Симуляція прогресії користувача
- ✅ Демонстрація зворотнього ланцюга
- ✅ Рекомендації щодо здоров'я
- ✅ Статистика системи

### Запуск тестів

```bash
python test_rule_engine.py
```

**Тести:**

- Unit тести для всіх класів
- Тести механізму виведення
- Інтеграційні тести
- Тести LifeQuest сценаріїв

### Програмне використання

```python
from rule_engine import create_rule_based_system, Fact, RuleParser
import json

# 1. Створення системи
kb, engine = create_rule_based_system()

# 2. Завантаження правил
with open('data/lifequest_rules.json', 'r') as f:
    rules = RuleParser.parse_json_rules(json.load(f))
    kb.add_rules(rules)

# 3. Додавання фактів
kb.add_fact(Fact('user_level', 0))
kb.add_fact(Fact('total_tasks_completed', 5))

# 4. Виконання виведення
result = engine.forward_chain()

# 5. Отримання результатів
if kb.has_fact('achievement_earned'):
    achievement = kb.get_fact('achievement_earned').value
    print(f"Досягнення: {achievement}")
```

---

## 📈 Приклади роботи

### Приклад 1: Новий користувач

**Вхідні дані:**

```python
user_level = 0
total_tasks_completed = 0
```

**Результат:**

```
✓ Правило R001 спрацювало
✓ Статус: Новачок
✓ Доступні квести: ["Перше завдання", "Знайомство з системою"]
```

### Приклад 2: Досягнення

**Вхідні дані:**

```python
total_tasks_completed = 5
has_achievement_first_steps = false
```

**Результат:**

```
✓ Правило R003 спрацювало
✓ Досягнення: Перші кроки
✓ Отримано +25 XP
```

### Приклад 3: Рекомендації здоров'я

**Вхідні дані:**

```python
sitting_hours_today = 7
physical_activity_today = 0
```

**Результат:**

```
✓ Правило R010 спрацювало
✓ Рекомендація: Фізична активність
✓ Квест: 20 хвилин ходьби
```

---

## 🎓 Технічні особливості

### Підтримувані оператори

```python
'=='     # Рівність
'!='     # Нерівність
'>'      # Більше
'<'      # Менше
'>='     # Більше або дорівнює
'<='     # Менше або дорівнює
'in'     # Входження в список
'not_in' # Не входження в список
'contains' # Містить елемент
```

### Пріоритети правил

- **100-80:** Критичні (статуси, рівні)
- **79-40:** Важливі (досягнення, здоров'я)
- **39-0:** Додаткові (персоналізація)

### Захист від циклів

- Максимум 100 ітерацій у forward chaining
- Максимум 50 рівнів глибини у backward chaining
- Відстеження відвіданих вузлів

---

## 📊 Результати тестування

### Unit тести

- ✅ TestFact: 3/3 пройдено
- ✅ TestCondition: 6/6 пройдено
- ✅ TestRule: 3/3 пройдено
- ✅ TestKnowledgeBase: 4/4 пройдено
- ✅ TestInferenceEngine: 4/4 пройдено
- ✅ TestIntegration: 1/1 пройдено

**Загалом: 21/21 тестів пройдено успішно**

### Демонстрації

- ✅ Forward chaining працює коректно
- ✅ Backward chaining доводить цілі
- ✅ Правила застосовуються за пріоритетом
- ✅ Нові факти створюються правильно

---

## 🔌 Інтеграція з LifeQuest

### Варіант 1: Як модуль

```python
# У вашому додатку
from rule_based_system.rule_engine import create_rule_based_system, Fact

class LifeQuestAI:
    def __init__(self):
        self.kb, self.engine = create_rule_based_system()
        self.load_rules()

    def update_user_state(self, user_data):
        # Оновлюємо факти з даних користувача
        for key, value in user_data.items():
            self.kb.add_fact(Fact(key, value))

        # Виконуємо виведення
        result = self.engine.forward_chain()

        return self.get_recommendations()

    def get_recommendations(self):
        recommendations = {}

        if self.kb.has_fact('achievement_earned'):
            recommendations['achievement'] = self.kb.get_fact('achievement_earned').value

        if self.kb.has_fact('suggested_quest'):
            recommendations['quest'] = self.kb.get_fact('suggested_quest').value

        return recommendations
```

### Варіант 2: Як API endpoint

```python
from flask import Flask, request, jsonify

app = Flask(__name__)
kb, engine = create_rule_based_system()

@app.route('/analyze', methods=['POST'])
def analyze_user():
    user_data = request.json

    # Додаємо факти
    for key, value in user_data.items():
        kb.add_fact(Fact(key, value))

    # Виконуємо аналіз
    result = engine.forward_chain()

    # Повертаємо рекомендації
    return jsonify({
        'status': kb.get_fact('user_status').value if kb.has_fact('user_status') else None,
        'recommendations': result
    })
```

---

## 📝 Висновки

### Досягнуті результати

1. ✅ **Реалізовано rule-based систему** з повним функціоналом:

   - Класи для представлення фактів, правил, умов і дій
   - База знань для зберігання інформації
   - Механізм логічного виведення

2. ✅ **Завантаження бази знань:**

   - 20 правил для різних аспектів геймифікації
   - Підтримка JSON формату
   - Гнучкий парсер для розширення

3. ✅ **Механізм логічного виведення:**

   - Forward chaining - від фактів до висновків
   - Backward chaining - від цілей до фактів
   - Пріоритизація правил
   - Захист від циклів

4. ✅ **Інтеграція з LifeQuest:**
   - Правила для системи досягнень
   - Рекомендації щодо здоров'я
   - Персоналізовані квести
   - Аналіз активності користувача

### Практичне застосування

Система може використовуватись для:

- 🎯 Автоматичного присвоєння статусів і досягнень
- 💪 Моніторингу здоров'я та самопочуття
- 📊 Аналізу продуктивності
- 🎮 Генерації персоналізованих квестів
- 🏆 Визначення винагород і бонусів

### Переваги реалізації

- **Модульність:** Легко додавати нові правила
- **Гнучкість:** Підтримка різних типів умов і операторів
- **Масштабованість:** Ефективна робота з великою кількістю правил
- **Тестованість:** Повне покриття unit тестами
- **Документованість:** Детальна документація коду

---

## 📚 Додаткові матеріали

### Структура файлів

```
rule_based_system/
├── rule_engine.py          # 400+ рядків коду
├── demo.py                 # 300+ рядків демонстрації
├── test_rule_engine.py     # 250+ рядків тестів
├── data/
│   ├── lifequest_rules.json    # 20 правил
│   └── initial_facts.json      # 26 фактів
└── README.md               # Документація
```

### Статистика

- **Всього коду:** 950+ рядків Python
- **Правил:** 20 шт
- **Фактів:** 26 шт
- **Тестів:** 21 шт
- **Покриття:** 100% основного функціоналу

---

## 👨‍💻 Автор

**Виконав:** [Ваше ім'я]  
**Група:** [Ваша група]  
**Дата:** 2024  
**Проект:** LifeQuest - Gamify Your Life

---

## 📄 Ліцензія

Цей код створено для освітніх цілей в рамках магістерської роботи.
